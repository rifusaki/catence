/** Paths owned by one isolated local Catence data store. */
export type CatencePaths = {
  root: string;
  database: string;
  raw: string;
  lake: string;
  staging: string;
  config: string;
  secrets: string;
  lock: string;
};
