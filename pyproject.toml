[project]
name = "catence-data"
version = "0.1.0"
description = "Local Catence provider workers for read-only fitness data extraction"
requires-python = ">=3.12"
dependencies = [
  "duckdb>=1.4.0",
  "fitdecode>=0.10.0",
  "garminconnect>=0.2.37",
  "polars>=1.0.0",
  "pyarrow>=18.0.0",
  "pydantic>=2.0.0",
  "stravalib>=2.5.0",
]

[tool.uv.sources]
# fork commit
garminconnect = { git = "https://github.com/rifusaki/python-garminconnect.git", rev = "732bf14602bce2ca22cb569ba6583c3229bf904d" }

[tool.uv]
package = false

[dependency-groups]
dev = ["pytest>=8.0.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["python/tests"]
